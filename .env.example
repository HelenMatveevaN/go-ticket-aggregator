# Application Setup
APP_ENV=development

# HTTP Server
HTTP_PORT=8080

# PostgreSQL Production Database URL
DATABASE_URL=postgres://user:password@host:port/dbname?sslmode=disable

# Kafka Brokers (JSON format for slice)
KAFKA_BROKERS=["localhost:9092"]
